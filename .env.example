# Flow Open API key — copy this file to `.env` and paste your key below.
# Issue a key from Flow's 키관리 (Key Management) page.
# `.env` is gitignored: your key never gets committed or shared.
FLOW_API_KEY=
