# Secrets — the user's API key lives here and must never be committed
.env
.env.local
*.key
# Plaintext key hand-off files imported by setup.mjs (deleted after import)
flow-key.txt
FLOW_KEY.txt
key.txt

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/
